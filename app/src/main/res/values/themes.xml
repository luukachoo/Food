<resources>
    <!-- Base application theme. -->
    <style name="Theme.App" parent="Theme.SplashScreen">
        <item name="colorPrimary">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="android:statusBarColor">@color/white</item>

        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_app_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.FoodApplication</item>
    </style>

    <style name="Theme.FoodApplication" parent="Base.Theme.FoodApplication" />
</resources>